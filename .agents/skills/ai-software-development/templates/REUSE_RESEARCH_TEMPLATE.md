# Reuse Research

Evaluate solutions in this order: existing project code; built-in framework/platform capability; official SDK; mature open source; managed service; custom code.

## Problem
What capability do we need?

## Candidates
For each candidate record type, official source, fit, compatibility, maintenance, security, license, dependency footprint, pros and cons.

## Decision
**Selected approach:**
**Reason:**
**Why reuse or custom code is justified:**

## Review trigger
Re-evaluate if the dependency becomes unmaintained, has a major security issue, the framework adds an equivalent built-in feature, or it becomes a maintenance/performance burden.
